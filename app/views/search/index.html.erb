<div class="container">
  <div class="row">
    <div class="col-sm-12">
      <% if @products.count == 0 %>
        <h1 class="red">No products were found for that search term</h1>
      <% end %>
      <h2>Results: <%= @products.count %></h2>
    </div>
  </div>
  <div class="row">
    <% @products.each do |product| %>
      <div class="col-sm-3 product" id="product-<%= product.name %>">
        <h5><%= product.name %></h5>
        <p>SKU: <%= product.sku %></p>
        <%= image_tag product.image, class: "product-image" %>
        <p><%= product.short_description %></p>
        <p>Customer Review Average: <%= product.customer_review_average %></p>
        <p>Sale Price: $<%= product.sale_price %></p>
      </div>
    <% end %>
  </div>
</div>
